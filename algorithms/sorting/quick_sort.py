from random import randint


def quick_sort(arr: list):
    """
        Quicksort algorithm applies the divide-and-conquer principle
        to divide the input array into two lists, the first list with
        small items and the second with large items.
        The algorithm then sorts the lists recursively.
    """
    pass
